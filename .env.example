# Copy this file to .env and fill in your key(s). .env is gitignored and is
# never read into source — demonclock/llm/config.py's GenerationConfig.from_env()
# loads it at startup; a real environment variable of the same name always
# takes precedence over whatever's in .env.

GEMINI_API_KEY=
